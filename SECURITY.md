# 安全说明

## 当前支持范围

TiPE 仍处于 0.1.0 开发预览阶段，目前只维护 `main` 分支。

## 建议私下报告的问题

- 密码或敏感输入框仍被监督、持久化或发送给模型。
- API Key 出现在命令行、日志或诊断输出。
- 模型输出可以绕过本地校验执行命令或写任意文件。
- 候选或监督协议导致越界读写、崩溃或权限逃逸。
- 安装/卸载脚本写出 `~/.local` 之外的非预期路径。

报告时请提供最小复现，不要发送真实密码、完整 API Key 或无关私人文本。当前没有单独的安全邮箱时，可以先通过仓库所有者的 GitHub 主页联系，并只公开不含利用细节的概要 Issue。

## 设计边界

- TiPE 只能读取 fcitx5 交付给当前输入上下文的事件，不是全局键盘记录器。
- fcitx5 标记为 `Password`、`Sensitive` 或客户端 `Disable` 的上下文会关闭监督和模型。
- 云端额外数据权限默认关闭。
- API Key 文件权限为 `0600`。
- 外部模型通过固定协议返回建议，结果必须经过本地验证。
